import { ScreenComponent } from "../Screen";
import SM from "./index.module.scss";

const Main = () => {
  return (
    <main className={SM.Main}>
      <ScreenComponent />
    </main>
  );
};

export default Main;
